# Credentials — never commit
.env.local
*.env
.env
client_secret_*.json

# Python
__pycache__/
*.pyc
.pytest_cache/

# Excel exports (use Monday API instead)
docs/*.xlsx

# Terraform — provider binaries and local state cache
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfplan

# Design assets — stored in Cloudinary, not in git
*.png
*.jpg
*.webp
Music_SFXs/
Music_SFXs.zip
docs/*.pdf
